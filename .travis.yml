osx_image: xcode9
language: swift
cache:
  - bundler
  - cocoapods
branches:
  only:
    - develop
    - master
notifications:
  email: false
before_install:
  - gem install bundler
  - brew update
  - bundle install && brew bundle
script: bundle exec fastlane funky
